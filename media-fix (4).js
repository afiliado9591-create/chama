(()=>{
  // Este arquivo foi desativado de propósito.
  // Tudo que ele fazia (botões de imagem/vídeo, áudio, envio de texto) já é
  // feito por chama-message-media-runtime-v2.js. Deixar este arquivo ativo
  // fazia ele recriar os ícones de câmera/vídeo toda vez que a página mudava,
  // brigando com o script novo e deixando os ícones antigos aparecerem de novo.
})();
