import { AwsClient } from "aws4fetch";
const FIREBASE_API_KEY="AIzaSyCcAVkmLUKPcEMZ5erDswbOQ8eO493pl2I";
const MAX={image:5*1024*1024,audio:10*1024*1024,video:25*1024*1024};
function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}})}
function kindFromType(type=""){if(type.startsWith("image/"))return"image";if(type.startsWith("audio/"))return"audio";if(type.startsWith("video/"))return"video";return""}
function extFromType(type=""){const map={"image/jpeg":"jpg","image/png":"png","image/webp":"webp","image/gif":"gif","audio/webm":"webm","audio/mpeg":"mp3","audio/mp4":"m4a","audio/ogg":"ogg","video/mp4":"mp4","video/webm":"webm","video/quicktime":"mov"};return map[type]||"bin"}
async function verifyFirebase(request){const h=request.headers.get("authorization")||"";const token=h.startsWith("Bearer ")?h.slice(7).trim():"";if(!token)return null;try{const r=await fetch(`https://identitytoolkit.googleapis.com/v1/accounts:lookup?key=${FIREBASE_API_KEY}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({idToken:token})});if(!r.ok)return null;const d=await r.json(),u=d.users?.[0];return u?.localId?{uid:u.localId,email:u.email||""}:null}catch{return null}}
export async function onRequestPost({request,env}){try{const user=await verifyFirebase(request);if(!user)return json({error:"Faça login novamente para enviar mídia."},401);const body=await request.json().catch(()=>({}));const type=String(body.type||"").split(";")[0].trim().toLowerCase();const kind=kindFromType(type);if(!kind)return json({error:"Formato não permitido."},415);if(!env.R2_ACCOUNT_ID||!env.R2_BUCKET_NAME||!env.R2_ACCESS_KEY_ID||!env.R2_SECRET_ACCESS_KEY)return json({error:"Upload direto R2 ainda não configurado no servidor."},503);const size=Number(body.size||0);if(!size||size>MAX[kind])return json({error:`Arquivo inválido ou maior que ${MAX[kind]/1024/1024} MB.`},413);const ext=extFromType(type);const key=`${user.uid}/${kind}/${Date.now()}-${crypto.randomUUID()}.${ext}`;const endpoint=`https://${env.R2_ACCOUNT_ID}.r2.cloudflarestorage.com`;const client=new AwsClient({accessKeyId:env.R2_ACCESS_KEY_ID,secretAccessKey:env.R2_SECRET_ACCESS_KEY,service:"s3",region:"auto"});const target=new URL(`${endpoint}/${env.R2_BUCKET_NAME}/${key}`);target.searchParams.set("X-Amz-Expires","900");const signed=await client.sign(new Request(target.toString(),{method:"PUT",headers:{"Content-Type":type}}),{aws:{signQuery:true}});return json({ok:true,key,kind,url:`/api/media?key=${encodeURIComponent(key)}`,uploadUrl:signed.url.toString(),contentType:type,expiresIn:900});}catch(e){console.error("media presign",e);return json({error:"Não foi possível preparar o upload direto."},500)}}
