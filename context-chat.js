import{getApps,initializeApp}from"https://www.gstatic.com/firebasejs/12.18.0/firebase-app.js";
import{getAuth,onAuthStateChanged}from"https://www.gstatic.com/firebasejs/12.18.0/firebase-auth.js";
import{getFirestore,collection,query,where,limit,getDocs,doc,setDoc,serverTimestamp}from"https://www.gstatic.com/firebasejs/12.18.0/firebase-firestore.js";
const cfg={apiKey:"AIzaSyCcAVkmLUKPcEMZ5erDswbOQ8eO493pl2I",authDomain:"chama-cfc28.firebaseapp.com",projectId:"chama-cfc28",storageBucket:"chama-cfc28.firebasestorage.app",messagingSenderId:"680045231088",appId:"1:680045231088:web:8db35684e4b56a320ebb35"};
const app=getApps()[0]||initializeApp(cfg),auth=getAuth(app),db=getFirestore(app);let me=null;
const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const money=v=>{const n=Number(String(v||'').replace(/[^0-9,.-]/g,'').replace('.','').replace(',','.'));return Number.isFinite(n)?n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'}):String(v||'')};
function style(){if(document.getElementById('ctxStyle'))return;const s=document.createElement('style');s.id='ctxStyle';s.textContent=`.ctx-btn{width:100%;border:1px solid #0b7a53;background:#eef9f4;color:#087650;padding:10px;border-radius:10px;font-weight:850;font-size:13px;margin-top:7px}.ctx-card{background:#fff;border-left:4px solid #0b7a53;border-radius:13px;padding:10px 12px;margin:5px auto 10px;max-width:min(94%,600px);box-shadow:0 1px 5px #0001}.ctx-card b{display:block;color:#173f33}.ctx-card small{display:block;color:#68766f;margin-top:3px}.ctx-card a{display:inline-block;margin-top:5px;color:#087650;font-weight:800}.ctx-q{display:flex;gap:6px;overflow:auto;padding:0 10px 8px}.ctx-q button{white-space:nowrap;border:1px solid #cde5da;background:#fff;color:#087650;border-radius:999px;padding:7px 10px;font-size:12px;font-weight:750}`;document.head.appendChild(s)}
function productFromCard(card){const title=card.querySelector('.product-title')?.textContent?.trim()||'Produto';const price=card.querySelector('.product-price')?.textContent?.trim()||'';const img=card.querySelector('img')?.src||'';const link=card.querySelector('a.buy')?.href||'';const badge=card.querySelector('.badge')?.textContent?.trim()||'';return{title,price,img,link,platform:badge}}
async function ownerFromCatalog(){const title=document.getElementById('catalogTitle')?.textContent||'';const emailMatch=title.match(/[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}/);if(emailMatch){const s=await getDocs(query(collection(db,'users'),where('email','==',emailMatch[0].toLowerCase()),limit(1)));let u=null;s.forEach(d=>u={uid:d.id,...d.data()});if(u)return u}const chatEmail=document.getElementById('chatEmail')?.textContent?.trim().toLowerCase();if(chatEmail){const s=await getDocs(query(collection(db,'users'),where('email','==',chatEmail),limit(1)));let u=null;s.forEach(d=>u={uid:d.id,...d.data()});if(u)return u}return null}
function chatId(a,b){return[a,b].sort().join('_')}
async function startContext(product){if(!me)return alert('Entre no Chama para conversar.');const owner=await ownerFromCatalog();if(!owner||owner.uid===me.uid)return alert('Abra o catálogo de outro usuário para iniciar uma conversa sobre o produto.');const id=chatId(me.uid,owner.uid);const context={type:'product',title:product.title,price:product.price||'',image:product.img||'',link:product.link||'',platform:product.platform||'',ownerId:owner.uid,startedBy:me.uid,updatedAt:Date.now()};await setDoc(doc(db,'chats',id),{participants:[me.uid,owner.uid],context,updatedAt:serverTimestamp()},{merge:true});sessionStorage.setItem('chamaContext',JSON.stringify({chatId:id,...context}));document.getElementById('closeCatalog')?.click();const row=[...document.querySelectorAll('#usersList .user')].find(r=>(r.querySelector('.user-email')?.textContent||'').trim().toLowerCase()===(owner.email||'').toLowerCase());if(row)row.click();setTimeout(renderContext,300)}
function enhance(){document.querySelectorAll('#catalogGrid .product').forEach(card=>{if(card.querySelector('.ctx-btn'))return;const b=document.createElement('button');b.className='ctx-btn';b.textContent='💬 Chamar sobre este produto';b.onclick=e=>{e.preventDefault();e.stopPropagation();startContext(productFromCard(card)).catch(err=>{console.error(err);alert('Não consegui abrir a conversa sobre este produto.')})};card.querySelector('.product-body')?.appendChild(b)})}
function renderContext(){document.querySelectorAll('.ctx-card,.ctx-q').forEach(x=>x.remove());let c=null;try{c=JSON.parse(sessionStorage.getItem('chamaContext')||'null')}catch{}if(!c)return;const messages=document.getElementById('messages');if(!messages)return;const card=document.createElement('div');card.className='ctx-card';card.innerHTML=`<b>💬 Conversa sobre: ${esc(c.title)}</b><small>${esc(c.platform)}${c.price?' • '+esc(c.price):''}</small>${c.link?`<a href="${esc(c.link)}" target="_blank" rel="noopener">Ver produto</a>`:''}`;messages.insertAdjacentElement('beforebegin',card);const q=document.createElement('div');q.className='ctx-q';['Ainda está disponível?','Quais tamanhos/variações?','Qual o prazo de entrega?','Como funciona o pagamento?'].forEach(t=>{const b=document.createElement('button');b.textContent=t;b.onclick=()=>{const i=document.getElementById('messageInput');if(i){i.value=t;i.focus()}};q.appendChild(b)});messages.insertAdjacentElement('beforebegin',q)}
function scan(){enhance();const active=document.getElementById('activeChat');if(active&&!active.classList.contains('hidden'))renderContext()}
style();new MutationObserver(()=>{clearTimeout(window.__ctxT);window.__ctxT=setTimeout(scan,80)}).observe(document.body,{childList:true,subtree:true,attributes:true,attributeFilter:['class']});document.addEventListener('click',()=>setTimeout(scan,100),true);onAuthStateChanged(auth,u=>{me=u||null;scan()});scan();
